<h1>Oldest director</h1>

<% director_query = Director.where.not({ :dob => nil }).order({ :dob => :asc }) %>

<% oldest_director = director_query.at(0) %>

<% oldest_director_dob = oldest_director.dob%>

<p>The oldest director in our dataset right now is <a href="/directors/<%= oldest_director.id %>"><%= oldest_director.name %></a>, born on <%= oldest_director.dob.strftime("%B %e, %Y.") %></p>
